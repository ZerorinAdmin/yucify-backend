# Required for Fly.io deployment (set via: fly secrets set KEY=value)
BACKEND_SECRET=your-secret-here

# Optional: persistent Facebook login (reduces login walls)
# ADSPY_FACEBOOK_PROFILE=/data/fb-profile

# Optional: run browser visible (default: headless)
# ADSPY_HEADLESS=false
